
void main() {
    // Variable
    int i = 0;

    // Constant
    //const int j = i;

    // Assignment
    // i = i + 1;
    //j = j + 1;

    // Conditional 1
    // if (i == 2) {
    //     i = i + 1;
    // }

    // // Conditional 2
    // if (i == 2) {
    //     i = i + 1;
    // } else {
    //     i = i + 1;
    // }

    // Conditional 3
    //int k = i == 2 ? i+1 : i - 1;

    // Print
    //print i;

    //boolean b = i > 10;

    // Iteration
    while (i < 10) {
        int k = 0;
        i = i + 1;
        print i;
        while (k < 5) {
            print k;
            k = k + 1;
        }
    }
}
